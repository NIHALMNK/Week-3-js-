//for remove last element in an array

//Given the array fruits = ['apple', 'banana', 'mango'], use pop() to remove the last element. What is the array after this operation?

let fruits = ['apple', 'banana', 'mango']

let r=fruits.pop()
console.log(fruits);//=>[ 'apple', 'banana' ]

//=====================================================

//You have an array colors = ['red', 'blue', 'green']. Use pop() to remove the last color and store it in a variable. What is the removed color?

let colors = ['red', 'blue', 'green'];
let removedColour=colors.pop()
console.log(removedColour); //=>'green

//========================================================
