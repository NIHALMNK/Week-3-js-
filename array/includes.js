//to check is it there
// Given the array colors = ['red', 'blue', 'green'], use includes() to check if 'blue' is in the array. What does the method return?
let colors = ['red', 'blue', 'green']
let r=colors.includes("blue")
console.log(r);//=>true
//==================================================
//ou have an array numbers = [1, 2, 3, 4]. Use includes() to check if the array contains the number 5. What is the result?

let numbers = [1, 2, 3, 4]
let isthere=numbers.includes(5)
console.log(isthere);//=>false
//===================================================
