//reverse();
//to convert to reverse
//===============================================
// Given the array numbers = [1, 2, 3, 4], use reverse() to reverse the order of the elements. What is the result?
let numbers = [1, 2, 3, 4]
let r=numbers.reverse();
console.log(r);//=>[ 4, 3, 2, 1 ]
//================================================
//You have an array letters = ['a', 'b', 'c', 'd']. Use reverse() to reverse the array. What does the reversed array look like?
let letters = ['a', 'b', 'c', 'd']
let rr=letters.reverse()

console.log(rr);//===>[ 'd', 'c', 'b', 'a' ]


