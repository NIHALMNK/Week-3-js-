//combine the words into a single string 
//words = ['Hello', 'world'], use join() to combine the words into a single string separated by a space. What is the result?


let words = ['Hello', 'world']
let jj=words.join('');
console.log(jj);//=>'Helloworld' 
//=======================================

//You have an array letters = ['a', 'b', 'c']. Use join() to join the letters with a hyphen (-). What does the result look like?
let letters = ['a', 'b', 'c']
let com=letters.join('');
console.log(com);//=>a,b,c
//========================================
